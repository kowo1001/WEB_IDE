import {} from '../actions';

const init_state = {
    groups: []
}

function openFileReducers (state=init_state, action) {
    switch(action.state) {
        default: break;
    }
    return state;
}

export default openFileReducers;